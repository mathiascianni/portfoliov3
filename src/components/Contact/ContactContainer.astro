---
import SendIcon from "../../components/General/Icons/SendIcon.astro";
import ContactContent from "./ContactContent";
---

<section class="min-h-screen relative z-[2] p-desk font-light">
    <ContactContent client:visible />
    <!-- <div class="text-4xl mx-desk">
            <ul class="grid grid-cols-2 gap-16 text-center">
                {socials.map((social) => <ContactButton social={social} />)}
            </ul>
        </div> -->
</section>
