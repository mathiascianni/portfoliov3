<header class="min-h-screen flex items-center justify-center relative z-[2]">

    <div class="flex flex-col text-xl text-center">
        <div class="mb-8 font-light">
            ¡Saludos! soy <h1 class="text-8xl font-title uppercase">
                Mathias Cianni
            </h1>
        </div>
        <p class="font-light mb-8" id="typewriter"></p>
        <img
            src="icons/downarrow.svg"
            alt="Move to next section"
            class="w-8 block mx-auto animate-bounce"
        />
    </div>
</header>
<div class="w-full h-[320px] bg-gradient-to-b from-primary/70 to-dark"></div>

<script>
    const text = document.getElementById("typewriter");
    const textToWrite =
        "Desarrollador frontend especializado en crear experiencias de usuario interactivas";
    const timeToWrite = 30; //MS

    function writeText() {
        let index = 0;
        const intervalId = setInterval(() => {
            text.innerHTML += textToWrite[index];
            index++;
            if (index >= textToWrite.length) {
                clearInterval(intervalId);
            }
        }, timeToWrite);
    }

    writeText();
</script>

